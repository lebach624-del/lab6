import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Disable Turbopack for production builds if needed, although Next 15 handles it better
  // We'll keep it simple for now but ensure it's a valid config
};

export default nextConfig;
